package com.xiaoxiaoowo.yuehua.system.handlePets;

import com.xiaoxiaoowo.yuehua.data.MonsterData;
import com.xiaoxiaoowo.yuehua.data.PetData;
import org.bukkit.Location;

public final class DoPetAttack {
    public static double doPetAttack(String id, MonsterData monsterData,PetData petData) {
        switch (id) {
            default -> {
                return 1.0d;
            }
        }
    }

    public static void doPetAttack(String id, Location location, PetData petData) {
        switch (id) {
        }
    }
}
